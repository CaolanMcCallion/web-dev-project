<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- Link to Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <!-- Navigation bar with links to dashboard, send message, and logout -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Academic Progression</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/admin/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/messages/send">Send Message</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Logout</a>
                </li>
            </ul>
        </div>
    </nav>

    <!-- Container for the messages table -->
    <div class="container mt-4">
        <h1 class="mb-4">Messages</h1>

        <!-- Table to display messages -->
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Sender</th>
                    <th scope="col">Recipient</th>
                    <th scope="col">Message</th>
                    <th scope="col">Date Sent</th>
                </tr>
            </thead>
            <tbody>
                <!-- Loop through all messages and display each message's details -->
                <% messages.forEach(message=> { %>
                    <tr>
                        <td>
                            <%= message.sender_id %>
                        </td> <!-- Sender ID -->
                        <td>
                            <%= message.recipient_id %>
                        </td> <!-- Recipient ID -->
                        <td>
                            <%= message.message_text %>
                        </td> <!-- Message Text -->
                        <td>
                            <%= message.sent_at %>
                        </td> <!-- Sent Date -->
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>